#include <iostream>
#include <cmath> 
#include "Header.h"
using namespace std;
int main()
{
    setlocale(LC_ALL, "Russian");
    int n;
    cout << "Введите значение n: ";
    cin >> n;
    if (!correct(n))//условие,если некорректно 
    {
        cout << "Ошибка: n - больше или равно 10." << endl;
        return 1; 
    }
    unsigned long long s = 0;//инициализируем переменную суммы,избегаем переполнение
    for (int i = 1; i <= n; ++i)
    {
        s += static_cast<unsigned long long>(pow(i, n)); 
    }
    cout << "Сумма 1^" << n << " + 2^" << n << " + ... + " << n << "^" << n << " = " << sum << endl;
    cout << "Программа завершена, нажмите пробел. ";
    return 0;
}
